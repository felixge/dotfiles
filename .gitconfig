[alias]
	ci = commit
	st = status
	co = checkout
	up = pull --rebase
	sup =! git stash && git up && git stash apply
[rerere]
	enabled = 1
[core]
	excludesfile = /Users/felix/.gitignore
[push]
	default = current
[include]
    path = ~/.gitconfig_secret
