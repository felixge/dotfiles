[alias]
	ci = commit
	st = status
	up = pull --rebase
	sup =! git stash && git up && git stash apply
[user]
       	name = Felix Geisendörfer
       	email = felix@debuggable.com
[rerere]
       	enabled = 1
